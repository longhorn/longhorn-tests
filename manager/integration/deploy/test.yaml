apiVersion: v1
kind: Pod
metadata:
  name: longhorn-test
  labels:
    longhorn-test: test-job
spec:
  containers:
  - name: longhorn-test-pod
    image: yasker/test
    imagePullPolicy: Always
    #args: ["-x", "-s", "-k", "test_ha"]
  restartPolicy: Never
